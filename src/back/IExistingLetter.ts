export interface IExistingLetter {
    letter: string,
    goodPosition: number[],
    badPosition: number[],
    noExists: number[]
}

export interface IResult {
    length: number,
    words: string
}
